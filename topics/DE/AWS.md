#AWS сервисы

1. Amazon Redshift – OLAP

Amazon Redshift — это облачное хранилище данных (DWH), предназначенное для быстрого анализа больших объемов данных

🔹 Ключевые особенности:

- MPP (Massively Parallel Processing) – обработка запросов параллельно на нескольких узлах.
- Колонночное хранилище – быстрее обрабатывает запросы, чем обычные реляционные базы данных.
- Поддержка SQL – совместим с PostgreSQL.
- Интеграция с S3, Glue, Kinesis, Athena – позволяет загружать и анализировать данные из других AWS-сервисов.

🛠 Когда использовать?

- Для анализа больших данных (Big Data).
- Когда нужна быстрая агрегация и обработка отчетов.
- Для бизнес-аналитики (BI).

Оптимизация загрузки данных в Redshift:

- COPY вместо INSERT – Redshift работает лучше, когда загружает большие батчи через команду COPY, а не INSERT.
- S3 as Staging Area – сначала записывать данные в S3, а затем загружать их в Redshift большими блоками.
- Partitioning в S3 – можно предварительно разбивать данные по дням/месяцам для более быстрой загрузки.

# Stored Procedures в Amazon Redshift

**Stored Procedures (хранимые процедуры)** в **Redshift** – это SQL-скрипты, объединенные в один блок, который можно
повторно выполнять. Они позволяют автоматизировать **загрузку данных**, **ETL-процессы** и **сложные вычисления**.

## 🔹 Основные возможности:

- **Используют PL/pgSQL** – аналог PostgreSQL, но с ограничениями Redshift.
- **Поддерживают переменные, циклы, IF, CASE, LOOP.**
- **Позволяют передавать параметры и динамически выполнять SQL.**
- **Работают в рамках одной транзакции**, если не настроен `AUTOCOMMIT`.

## 🚀 Пример создания процедуры для COPY:

```sql
CREATE OR REPLACE PROCEDURE load_sales_data()
LANGUAGE plpgsql
AS $$
BEGIN
    EXECUTE 'COPY sales_data
             FROM ''s3://my-bucket/sales/''
             IAM_ROLE ''arn:aws:iam::123456789012:role/RedshiftRole''
             FORMAT AS PARQUET';
END;
$$;
```

📌 Как вызвать:

CALL load_sales_data();

🎯 Когда использовать?

✅ Автоматизация COPY FROM S3.
✅ Упрощение ETL/ELT-процессов.
✅ Передача динамических параметров.
✅ Обработка ошибок через TRY…CATCH.

2. AWS CloudWatch – сервис для мониторинга ресурсов и логирования событий в AWS.

🔹 Ключевые возможности:

- Мониторинг метрик (CPU, RAM, дисковая нагрузка).
- Алармы (CloudWatch Alarms) – триггеры на метрики (например, CPU > 80%).
- Логирование (CloudWatch Logs) – сбор и анализ логов приложений и сервисов.
- Автоматические действия – например, запуск Auto Scaling, если нагрузка растет.

🛠 Когда использовать?

- Для мониторинга EC2, Lambda, RDS и других AWS-ресурсов.
- Для тревожных уведомлений о сбоях или высокой нагрузке.
- Для анализа логов приложений.

3. CloudShell – терминал в браузере

🔹 Ключевые возможности:

- Запуск AWS CLI без установки на локальный компьютер.
- Предустановленные инструменты (Python, Git, Terraform).
- Доступ к IAM-авторизованным ресурсам (например, S3, EC2).

🛠 Когда использовать?

- Если нет доступа к локальному AWS CLI.
- Для быстрого администрирования AWS.
- Для тестирования и отладки команд.

4. AWS Lambda – serverless-функции

🔹 Ключевые возможности:

- Запуск кода в ответ на события (например, HTTP-запрос, загрузка файла в S3).
- Поддержка множества языков (Java, Python, Node.js, Go и др.).
- Автоматическое масштабирование – AWS сам увеличивает или уменьшает количество исполняемых функций.
- Оплата только за использованные ресурсы.

🛠 Когда использовать?

- Автоматическая обработка событий (загрузка файлов, обновление данных).
- API-бэкенды для микросервисов.
- Автоматизация инфраструктуры.

5. IAM (Identity and Access Management) – система управления пользователями, ролями и правами доступа в AWS.

🔹 Ключевые возможности:

- Пользователи, группы, роли – управление аккаунтами и разрешениями.
- Политики (Policies) – правила доступа, основанные на JSON.
- MFA (Многофакторная аутентификация) – дополнительная безопасность.
- IAM Roles – временные права доступа для сервисов (например, Lambda может работать с S3).

🛠 Когда использовать?

- Для разграничения доступа к ресурсам AWS.
- Для безопасности при работе с AWS API.
- Для настройки сервисных учетных записей.

6. AWS Health Dashboard – панель мониторинга состояния сервисов AWS.

🔹 Ключевые возможности:

- Мониторинг инцидентов и простоев в AWS.
- Уведомления о проблемах с конкретными регионами.
- История работоспособности сервисов (можно посмотреть проблемы в прошлом).

🛠 Когда использовать?

- Если сервис AWS работает нестабильно и нужно проверить статус.
- Для оповещения об инцидентах и плановых работах.
- Для разбора инцидентов и поиска причин сбоев.

7. AWS Secrets Manager – сервис для хранения и управления чувствительными данными (пароли, API-ключи, сертификаты).

🔹 Ключевые возможности:

- Безопасное хранение секретов (паролей, API-ключей).
- Автоматическая ротация паролей (например, обновление пароля в базе данных).
- Интеграция с RDS, Lambda, EC2.

🛠 Когда использовать?

- Хранение паролей и API-ключей в безопасном виде.
- Защита конфиденциальных данных в микросервисах.
- Автоматическое обновление секретов без простоя сервисов.

8. Amazon S3 – Object Storage, предназначенное для хранения файлов, резервных копий и логов.

🔹 Ключевые возможности:

- Объектное хранилище – файлы до 5 ТБ.
- Разные классы хранения (Standard, IA, Glacier).
- Версионирование – хранение старых версий файлов.
- Политики доступа (Bucket Policy, ACL).

🛠 Когда использовать?

- Хранение логов, бэкапов, статических файлов.
- Интеграция с CloudFront для раздачи контента.
- Долговременное хранение архивов и медиафайлов.

9. AWS Certificate Manager (ACM) – управление SSL/TLS сертификатами

🔹 Ключевые возможности:

- Бесплатные сертификаты для доменов в AWS.
- Автоматическое продление сертификатов.
- Интеграция с ALB, CloudFront, API Gateway.

🛠 Когда использовать?

- Настройка HTTPS для веб-сайтов и API.
- Защита внутреннего трафика в AWS.
- Управление сертификатами без ручного обновления.

10. Amazon Kinesis - полностью управляемая альтернатива Kafka.

- Используется для реального времени ingestion & streaming
- Поддерживает Exactly-Once Processing через Flink + Kinesis Data Analytics
- Интегрируется с Lambda, S3, DynamoDB

🔸 Kafka vs Kinesis:

- Kinesis проще в управлении (AWS управляет инстансами)
- Kafka более гибкий и поддерживает большую пропускную способность

11. AWS DMS (Database Migration Service) — сервис для миграции данных в реальном времени.

- Поддерживает CDC (Change Data Capture) — отслеживание изменений в БД
- Интегрируется с Flink через Flink CDC Connectors
- Подходит для репликации данных из MySQL, PostgreSQL, MongoDB в S3, Redshift, Kafka

💡 Когда использовать?

- Если нужно собирать изменения из реляционных БД в Flink и Kafka
- Если требуется бесшовная миграция данных между хранилищами

- CDC (Change Data Capture)

Это способ отслеживания изменений в базе данных (добавление, обновление или удаление данных) в реальном времени.
Он позволяет быстро передавать эти изменения в другие системы, например, для обновления хранилища данных.

- ODC (Operational Data Change)

Это изменения данных в операционных системах (в базах данных, которые обрабатывают текущие транзакции).
Это процесс обновления или перемещения данных между системами, но не всегда в реальном времени.

https://habr.com/ru/company/sberbank/blog/679028/

12. AWS Glue - serverless ETL-сервис с поддержкой Apache Spark.

- Может подготовить данные перед загрузкой в Flink
- Может читать данные из Flink и записывать в S3, Redshift, Snowflake
- Интегрируется с AWS Glue Data Catalog для хранения метаданных

💡 Flink vs Glue

- Glue — для batch ETL и предобработки данных
- Flink — для real-time stream processing

13. AWS Athena - serverless SQL-инструмент для анализа данных в S3.

- Позволяет выполнять SQL-запросы по данным в S3
- Поддерживает Parquet, ORC, JSON, CSV
- Интегрируется с Flink как источник данных (source) и приемник (sink)

💡 Когда использовать?

- Если данные уже хранятся в S3 и нужно быстро анализировать их SQL-запросами
- Если нужен BI-инструмент для аналитики данных в Flink

14. AWS Step Functions – serverless сервис для оркестрации рабочих процессов в AWS. Позволяет визуально моделировать
    последовательность выполнения задач (Lambda, Glue) с контролем состояний и ошибок.

Ключевые особенности:

- Автоматическое управление последовательностью выполнения задач.
- Глубокая интеграция с AWS (Lambda, S3, DynamoDB, SQS, SNS).
- Гибкость – поддержка параллельных, последовательных и условных ветвлений.
- Повторные попытки и обработка ошибок встроены в сервис.
- Оплата только за выполненные шаги (а не за постоянно работающий сервер).

Когда использовать AWS Step Functions?

- Автоматизация ETL-пайплайнов, обработки данных, машинного обучения.
- Управление микросервисными архитектурами без сложного кода.
- Создание мультиэтапных workflows с разными AWS-сервисами.

💡 Если нужен управляемый и отказоустойчивый процессинг задач в AWS – Step Functions идеальны!

---

# **AWS-сервисы для контейнеризации**

## **ECS (Elastic Container Service)**

**Описание:**  
ECS — это собственный сервис AWS для управления контейнерами, который позволяет запускать контейнеры на EC2 или
Fargate.  
AWS берет на себя управление кластером, обеспечивая масштабируемость и интеграцию с другими сервисами.

**Когда использовать?**

- Если нужен **полностью управляемый сервис** для Docker-контейнеров.
- Когда не требуется гибкость Kubernetes, а важно быстро развернуть контейнерные приложения.

---

## **EKS (Elastic Kubernetes Service)**

**Описание:**  
EKS — это полностью управляемый сервис Kubernetes в AWS, который позволяет развертывать и управлять контейнерными
приложениями в Kubernetes-окружении.

**Когда использовать?**

- Если требуется **гибкость и мощь Kubernetes**, но без ручного управления мастер-нодами.
- Когда проект уже использует Kubernetes, и требуется его интеграция с AWS.

---

## **Fargate**

**Описание:**  
Fargate — это **serverless-решение** для запуска контейнеров без управления серверами. Оно работает совместно с ECS и
EKS, убирая необходимость настраивать инфраструктуру.

**Когда использовать?**

- Если **не хочется управлять серверами** и важно просто запускать контейнеры.
- Когда нужна **автоматическая масштабируемость** без забот о кластере.

---

🎯 **Вывод:**

- **ECS** – простой и удобный сервис для управления контейнерами в AWS.
- **EKS** – лучший выбор, если уже используете Kubernetes.
- **Fargate** – оптимальный вариант, если хотите запускать контейнеры без управления серверами. 🚀

---

# Сравнение Stateful и Stateless сервисов

| **Характеристика**         | **Stateful**                                        | **Stateless**                                                 |
|----------------------------|-----------------------------------------------------|---------------------------------------------------------------|
| **Хранение данных**        | Данные хранятся внутри сервиса                      | Данные не хранятся внутри сервиса                             |
| **Простота разработки**    | Сложнее из-за управления состоянием                 | Проще - нет необходимости сохранять состояние                 |
| **Производительность**     | Быстрее для связанных операций                      | Может требовать дополнительной обработки данных               |
| **Масштабируемость**       | Труднее масштабировать из-за хранения данных        | Легче масштабировать - сервисы независимы                     |
| **Использование ресурсов** | Требует больше ресурсов                             | Использует меньше ресурсов                                    |
| **Передача данных**        | Возможность передавать данные внутри сервиса        | Данные передаются между сервисами                             |
| **Применение**             | Базы данных, кеши (Redis, PostgreSQL, Kafka)        | REST API, микросервисы, Lambda-функции                        |
| **Минусы**                 | Дополнительных ресурсы, Ухудшает производительность | Не для сложной логики, Передача больше данных между сервисами |
| **Примеры**                | AWS, Kubernetes, VM, Физические серверы             | AWS Lambda, Google Cloud Functions, Azure Functions           |

## Когда использовать?

- **Stateful** – когда нужно **сохранение состояния** (БД, сессии, обработка последовательных данных).
- **Stateless** – если важна **масштабируемость и отказоустойчивость** (API, микросервисы, обработка событий).
