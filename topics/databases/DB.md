# SQL BD

**Реляционная база данных (SQL)** — база, где данные хранятся в формате таблиц, они строго структурированы и связаны
друг с другом. В таблице есть строки и столбцы, каждая строка представляет отдельную запись, а столбец — поле с
назначенным ей типом данных. В каждой ячейке информация записана по шаблону.

**Преимущества SQL**:

- соответствия требованиям ACID
- целостность данных
- структурированность

**Недостатки SQL:**

- реляционные базы плохо масштабируются, с ними крайне сложно создавать распределенные хранилища
- проектирование крупных баз с множеством компонентов требует значительных усилий. Это приведение сущностей к нормальным
  формам и сложности в отображении связей типа многие-ко-многим. Такие схемы тяжело читать и понимать их
  бизнес-применение
- эволюция схемы данных почти всегда отстает от новых потребностей бизнеса. Миграция на обновленную схему занимает много
  времени, в течение которых сервер недоступен

**Что быстрее SQL или NoSQL:**  
NoSQL — название всех технологий, которые не являются SQL, БД документов, БД ключей-значений, БД событий.
Это разный функционал, все зависит от задач.

**Примеры:**

- Oracle
- MySQL
- SQLServer
- PostgreSQL

------------
**Нормализация** —
процесс организации данных, цель которого избежать дублирования и избыточности.

Преимущества:

- Лучшая организация базы данных
- Больше таблиц с небольшими строками
- Эффективный доступ к данным
- Большая гибкость для запросов
- Быстрый поиск информации
- Проще реализовать безопасность данных
- Позволяет легко модифицировать
- Сокращение избыточных и дублирующихся данных
- Более компактная база данных
- Обеспечивает согласованность данных после внесения изменений

**Нормальные формы**

1НФ:

- В каждой клетке таблицы только одно значение
- Не должно быть повторяющихся строк

2НФ:

- 1НФ
- Есть первичный ключ
- Все атрибуты зависят от первичного ключа, а не от какой-то ее части

3НФ:

- 2НФ
- Все атрибуты зависят от первичного ключа, но не от других атрибутов

------------
**Денормализация** — повышение производительности, через упрощение доступа к данным или через создание таблиц с
результатами отчетов.

**DIMENSIONAL MODELING** – это метод структуры данных, оптимизированный для хранения данных в хранилище данных. Целью
размерной модели является оптимизация базы данных для быстрого поиска данных.

- Схема Звезда
- Схема Снежинка

------------

# **Транзакции** —

Объединение операций, для выполнения всех, либо ни одной.

**Транзакции в SQL**

- BEGIN — создать новую транзакцию
- COMMIT — сохранить изменения в БД
- ROLLBACK — отменить изменения

## **Уровни изоляции транзакций**

| **Уровень**          | **Dirty Read** | **Non-repeatable Read** | **Phantom Read** | **Полная изоляция** |
|----------------------|----------------|-------------------------|------------------|---------------------|
| **READ UNCOMMITTED** | ✅ Возможны     | ✅ Возможны              | ✅ Возможны       | ❌ Нет               |
| **READ COMMITTED**   | ❌ Нет          | ✅ Возможны              | ✅ Возможны       | ❌ Нет               |
| **REPEATABLE READ**  | ❌ Нет          | ❌ Нет                   | ✅ Возможны       | ❌ Нет               |
| **SERIALIZABLE**     | ❌ Нет          | ❌ Нет                   | ❌ Нет            | ✅ Да                |

### **Описание аномалий при недостаточной изоляции**:

- **Потерянное обновление** – две параллельные транзакции изменяют одни и те же данные, но итоговый результат зависит от
  порядка их выполнения.
- **Грязное чтение (Dirty Read)** – запрос возвращает **неподтверждённые** данные, которые могут быть отменены через
  `ROLLBACK`.
- **Неповторяемое чтение (Non-repeatable Read)** – между двумя `SELECT` другой поток **изменил или удалил** строки.
- **Фантомное чтение (Phantom Read)** – между `SELECT` другой поток **добавил** новые строки.

> **Чем выше уровень изоляции, тем выше нагрузка на систему и ниже производительность.**

------------
**ACID:**

| **Свойство**                      | **Что значит**                                    | **Как это достигается в SQL?**                     |
|-----------------------------------|---------------------------------------------------|----------------------------------------------------|
| **Атомарность (Atomicity)**       | Все изменения успешны либо ни одно не выполняется | `ROLLBACK` при ошибке                              |
| **Согласованность (Consistency)** | Данные всегда остаются корректными                | Ограничения (`PRIMARY KEY`, `FOREIGN KEY`)         |
| **Изолированность (Isolation)**   | Транзакции не оказывают влияние друг на друга     | Уровни изоляции (`SERIALIZABLE`, `READ COMMITTED`) |
| **Устойчивость (Durability)**     | Изменения сохраняются даже после сбоя             | Лог транзакций (`WAL` в PostgreSQL)                |

------------

### **📌 Индексы в базах данных**

**Порядок полей в индексе** играет ключевую роль в производительности запросов. Грамотное построение индекса **ускоряет
фильтрацию, сортировку и группировку данных**.

### **🔹 Как правильно выбирать порядок полей?**

1️⃣ **Сначала самые селективные поля**

- Чем **меньше** строк остаётся после фильтрации по полю, тем **лучше**.
- **Пример:** Если `user_id` фильтрует до 10 записей, а `status` до 1000, то **`user_id` должно быть первым**.

2️⃣ **Поля, используемые в `WHERE`**

- Если запрос использует **несколько полей в `WHERE`**, порядок в индексе **должен совпадать** с их приоритетом
  фильтрации.

3️⃣ **Поля для `ORDER BY`**

- Если индекс **совпадает** с полями сортировки (`ORDER BY`), БД не тратит ресурсы на доп. сортировку.

4️⃣ **Поля для `GROUP BY`**

- Если `GROUP BY` использует индексированные поля, группировка выполняется быстрее.

------------

**Индекс для подмножества (Partial Index)** —
Индекс, который создается только для подмножества строк таблицы, удовлетворяющих определенному условию. Это позволяет
уменьшить размер индекса и повысить производительность запросов, которые часто обращаются к этому подмножеству данных.

значительно улучшают производительность поиска данных. Индекс создается на основе значений одного или нескольких
столбцов таблицы и содержит указатели на соответствующие строки, что позволяет быстро находить данные, соответствующие
заданным критериям.

- Уникальный индекс (Unique Index): Гарантирует, что значения в индексируемом столбце не будут повторяться. При наличии
  первичного ключа уникальный индекс может быть создан автоматически.
- Кластеризованный индекс (Clustered Index): Определяет физический порядок строк в таблице на основе значений ключа.
  Каждая таблица может иметь только один кластеризованный индекс.
- Некластеризованный индекс (Non-Clustered Index): Не изменяет физический порядок строк в таблице, но поддерживает
  логический порядок данных. Таблица может иметь множество некластеризованных индексов.

Индексы могут быть реализованы с использованием различных структур данных. Наиболее распространены B*-деревья,
B+-деревья, B-деревья и хеши.

**Проблемы:**  
Избыточное количество индексов может замедлить операции INSERT, UPDATE и DELETE строк, так как при этом необходимо
обновлять сами индексы. Кроме того, индексы требуют дополнительного объема памяти.

------------

### **Connection Pool – что это?**

**Connection Pool** — это **пул заранее созданных соединений с базой данных**, который **управляет их повторным
использованием**.

**Почему нужен?**

- **Создание нового соединения занимает время**.
- **Если делать подключение на каждый запрос**, производительность падает.

**Как работает?**

1. При первом запросе **пул создаёт соединение** с БД.
2. **При закрытии соединение не уничтожается, а возвращается в пул**.
3. **Следующий запрос получает уже готовое соединение, а не создаёт новое** .

**Плюсы:**

1. Быстрее, чем постоянные подключения
2. Снижает нагрузку на БД
3. Управляет количеством соединений

------------

|                            | **Optimistic Locking**                                                                                                                                                                                      | **Pessimistic Locking**                                                                                                                                                                           |
|----------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Как работает?**          | Проверка версии (`@Version`)                                                                                                                                                                                | Блокировка (`SELECT ... FOR UPDATE`)                                                                                                                                                              |
| **Производительность**     | 🚀 Высокая                                                                                                                                                                                                  | 🐢 Низкая (ожидание блокировки)                                                                                                                                                                   |
| **Конфликты?**             | ✅ Возможны, но без ожидания                                                                                                                                                                                 | ❌ Нет, но транзакции могут ждать                                                                                                                                                                  |
| **Когда использовать?**    | **Редкие конфликты** (например, обновление профиля)                                                                                                                                                         | **Частые конфликты** (например, бронирование билетов)                                                                                                                                             |
| **Пример в реальном мире** | Два пользователя редко редактируют один профиль одновременно, поэтому если один изменил данные, а второй сделал это позже, система просто уведомит его, что данные устарели, и он должен обновить страницу. | Два пользователя пытаются забронировать одно и то же место в самолёте, то первый, кто забронировал, получает блокировку, а остальные должны ждать её снятия, чтобы не было двойного бронирования. |

------------

### **Типы масштабирования в БД**

| **Тип масштабирования**                          | **Описание**                                                                              | **Преимущества**                                                                | **Недостатки**                                                               |
|--------------------------------------------------|-------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------|------------------------------------------------------------------------------|
| **Вертикальное масштабирование (Scaling Up)**    | Увеличение ресурсов одного сервера (RAM, CPU, диск)                                       | Простота реализации, не требует изменения архитектуры                           | Ограниченное масштабирование, высокая стоимость оборудования                 |
| **Горизонтальное масштабирование (Scaling Out)** | Добавление новых серверов в кластер базы данных                                           | Высокая масштабируемость, дешёвое оборудование                                  | Сложность настройки и управления кластером, изменение архитектуры приложения |
| **Шардинг (Sharding)**                           | Разделение БД на несколько частей, хранящихся на разных серверах                          | Эффективное использование ресурсов, возможность горизонтального масштабирования | Сложность настройки и управления, изменение архитектуры приложения           |
| **Партиционирование (Partitioning)**             | Разделение одной таблицы на логические части (партиции), которые могут храниться отдельно | Улучшает производительность запросов, снижает нагрузку на индекс                | Требует настройки, возможны сложности при изменении структуры данных         |
| **Репликация (Replication)**                     | Создание копий БД на нескольких серверах для распределения нагрузки и отказоустойчивости  | Высокая доступность данных, улучшенная производительность чтения                | Сложность настройки и синхронизации данных                                   |
| **Кэширование (Caching)**                        | Использование промежуточного слоя для хранения часто запрашиваемых данных                 | Улучшенная производительность, снижение нагрузки на БД                          | Необходимость управления кэшем, возможные проблемы с консистентностью данных |